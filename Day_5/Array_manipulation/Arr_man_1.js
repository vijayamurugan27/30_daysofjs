const arr = Array() // creates an an empty array
console.log(arr)

const eightEmptyValues = Array(8) // it creates eight empty values
console.log(eightEmptyValues)


const arr_1 = Array(8).fill('X') // it creates eight empty values
console.log(arr_1)

const arr_2 = Array(10).fill('0') // it creates eight empty values
console.log(arr_2)

const arr_3 = Array(4).fill('40') // it creates eight empty values
console.log(arr_3)
